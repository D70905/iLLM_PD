# Figures

`final/` contains the current manuscript-ready figure exports copied from the
working repository. `source/` contains plotting scripts and source data used for
the revised figures.

Historical exploratory plots may still exist in `plot/`; reviewer-facing figure
files should be taken from `figures/final/`.
